<input
  type="radio"
  id="{{ .Id }}"
  name="{{ .Name }}"
  class="ons-radio__input ons-js-radio"
  value="{{ .Value }}"
  {{ if .Checked }}
    checked
  {{ end }}
>  
{{ if .LocaleKey }}
  {{ template "partials/label" .}}
{{ end }}
